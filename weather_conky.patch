55a56,59
> # # # # # 
> # <mazienho@gmx.de> 2014-10-23
> # Change direction from degrees to string
> # # # # #
57c61,79
< # echo -e "Aktuelles Wetter - ${city}"
---
> newdirection=$(echo $direction | sed s/[^0-9]*//g)
> if [ $newdirection -ge 23 -a $newdirection -le 67 ]; then
> 	richtung="NO"
> elif [ $newdirection -ge 68 -a $newdirection -le 112 ]; then
> 	richtung="O"
> elif [ $newdirection -ge 113 -a $newdirection -le 157 ]; then
> 	richtung="SO"
> elif [ $newdirection -ge 158 -a $newdirection -le 202 ]; then
> 	richtung="S"
> elif [ $newdirection -ge 203 -a $newdirection -le 247 ]; then
> 	richtung="SW"
> elif [ $newdirection -ge 248 -a $newdirection -le 292 ]; then
> 	richtung="W"
> elif [ $newdirection -ge 293 -a $newdirection -le 337 ]; then
> 	richtung="NW"
> else 
> 	richtung="N"
> fi
> #echo -e "Aktuelles Wetter - ${city}"
61c83,84
< echo -e "Wind: ${speed}, ${direction}\n"
---
> #echo -e "Wind: ${speed}, ${direction}\n"
> echo -e "Wind: ${speed}, ${richtung}\n"
